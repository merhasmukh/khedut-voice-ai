services:
  qdrant:
    image: qdrant/qdrant:latest
    container_name: khedut_qdrant
    restart: always
    ports:
      - "6333:6333"  # HTTP REST API & Web Dashboard UI
      - "6334:6334"  # gRPC API
    volumes:
      - ./qdrant_storage:/qdrant/storage
    environment:
      - QDRANT__SERVICE__ENABLE_CORS=true
